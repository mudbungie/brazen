+++
title = "openai_responses: verify reasoning_summary wire shape + decide one-block vs per-part Thinking representation"
created = 1781663531
updated = 1781664204
claimant = "mark"
priority = 40
tags = ["test"]
+++
VERIFIED + DECIDED (bl-410e). Wire shape checked against OpenAI's published Responses streaming reference (authoritative wire contract; no live capture — no API key in env, and a live capture only re-confirms published field names, not the representation decision).

1. WIRE SHAPE — CONFIRMED. response.reasoning_summary_text.delta = {type, item_id, output_index, summary_index, delta, sequence_number}. summary_index, NO content_index. So summary deltas route to pair (output_index, 0) — the Thinking block opened on the reasoning item add. The fix in src/protocol/openai_responses/decode/mod.rs (item_added reasoning arm + part_key reading content_index → absent → 0) is correct. The hand-authored test (a_reasoning_item_opens_a_thinking_block_its_summary_delta_routes_in) matches the real wire. No revisit needed.

2. REPRESENTATION — KEEP ONE-BLOCK COLLAPSE. summary_index parts concatenate into the single canonical (output_index,0) Thinking block; ContentKind::Thinking {} has no part slot, matching Anthropic. No consumer need for per-part. The symmetric per-part path is confirmed real (sibling response.reasoning_summary_part.added carries summary_index, paralleling content_part.added's content_index) but NOT built — deferred to a real consumer need + a part-count capture (count does not change the collapse's losslessness).

3. NEW FINDING — second reasoning channel. response.reasoning_text.{delta,done} streams RAW reasoning and DOES carry content_index (verified-org/include-gated, usually not user-visible). Not in the §3.4 dispatch → falls through to []. Deliberate v0.1 narrowing (empty-set rule); recorded as CR-R4.

Artifacts: specs/providers.md §3.4 prose + new §7 CR-R4; test comment in tests/responses_decode_errors.rs upgraded from 'assumed' to 'verified'.