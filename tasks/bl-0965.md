+++
title = "Shared SSE/NDJSON/Identity decoder with rechunking determinism"
created = 1781559061
updated = 1781559061
priority = 68
tags = ["impl"]
+++
Implement protocol/sse.rs: the SseDecoder (blank-line frames, event:/data: extraction, partial-frame + partial-UTF-8 buffering), the NDJSON line-framer, and the Identity framer; wire them to Framing::decoder(). Implement the rechunker test harness (OneByte/MidData/MidUtf8/MidJsonNumber/WholeFixture) and the universal-invariant property tests (exactly one End; ContentStart/Delta/Stop well-formedness). 100% covered from literal/byte fixtures.