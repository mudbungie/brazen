+++
title = "OpenAI chat/completions Protocol impl (encode + decode)"
created = 1781559065
updated = 1781636638
claimant = "Merging"
priority = 65
tags = ["impl"]

[[blockers]]
id = "bl-ee8d"
on = "claim"

[[blockers]]
id = "bl-25bf"
on = "claim"
+++
Implement protocol/openai_chat.rs per spec 0002: encode (nested function tools, tool_choice spellings, content string-or-array, stream_options include_usage) and decode (positional choices[0].delta state machine, synthesized ContentStart{ToolUse}, arguments->JsonDelta, finish_reason mapping, final usage chunk, data:[DONE]->End, 4xx/5xx body parse). Record golden fixtures (basic, tools, 4xx/401). Then write the executable single-source-of-truth property test asserting the OpenAI-basic and Anthropic-basic fixtures decode to the same Vec<Event>.