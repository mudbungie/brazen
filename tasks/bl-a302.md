+++
title = "Ingress follow-up: config-side exports the listener lane could not reach (--in lossy_overrides validation gap)"
created = 1783746505
updated = 1783746505
priority = 12
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Two API insufficiencies reported by the bl-6cb4 lane, both config-side, one correctness gap. (1) KNOWN_ADAPTATIONS is private to config with no re-export, so the --in path cannot validate lossy_overrides names when an [ingress] table is present but not serve-complete: a typo'd override on --in is silently inert TODAY — violating config.md's own rule that a typo'd override must never silently leave the default in force (--serve validates fully via resolve_ingress; only --in leaks). Fix: export the validation (or a validate_lossy_overrides fn) from config and call it on the --in path (src/run/masq/filter.rs); test: typo'd override + --in = Config 78. (2) src/config/resolve/mod.rs still gates pub(crate) use ingress::IngressConfig behind cfg(test) with a comment saying the listener ball should lift it, and config/resolve/ingress.rs carries now-stale allow(dead_code)s — lift the gate, drop the allows. Small, single lane, touches src/config/* + src/run/masq/filter.rs + tests.