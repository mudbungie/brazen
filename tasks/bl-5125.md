+++
title = "Wire config-sourced system prompt: add system to PartialConfig/ResolvedConfig/fill_absent"
created = 1781649332
updated = 1781649332
priority = 56
tags = ["impl"]
+++
config.md ALREADY specs this (§4 line 209: req.system = req.system.take().or_else(|| cfg.system.clone()); §7 line 319 lists system among the resolved gen defaults fill_absent reads), and architecture.md §4.4/§5.5/Decision 10 treat req.system as the config-/flag-/file-sourced ergonomic leading prompt. The code lags:

- src/config/partial.rs — PartialConfig has no `system` field.
- src/config/resolve.rs — ResolvedConfig has no `system`; fill_absent (resolve.rs ~208-215) fills only model/max_tokens/temperature/top_p, not system.

Add the field through the schema, the fold, and fill_absent so a config/flag/file system prompt reaches encode. Not a harness blocker (a caller can already put system in the piped CanonicalRequest body), purely the ergonomic convenience the spec promises. Keep 100% coverage.