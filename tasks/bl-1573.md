+++
title = "Document the release-plz one-time prerequisite the live run exposed: enable 'Allow GitHub Actions to create and approve pull requests' (else the release-pr job 403s)"
created = 1781932162
updated = 1781932162
priority = 1
tags = ["docs"]
+++
First push to origin/main ran Release-plz; the publish job correctly SKIPPED (workflow_dispatch-gated), but the release-pr job failed 403: 'GitHub Actions is not permitted to create or approve pull requests.' That's a repo setting, not a code bug. Document it so the setup checklist is complete.

## Edits
- README §Releasing one-time setup: add — enable Settings -> Actions -> General -> Workflow permissions -> 'Allow GitHub Actions to create and approve pull requests' (the simplest fix), OR provide RELEASE_PLZ_TOKEN (a PAT, which also re-triggers CI) — either lets the release PR be opened.
- release-plz.yml header comment: note the same prerequisite next to the RELEASE_PLZ_TOKEN line.

## Close gate
make check green; YAML parses.