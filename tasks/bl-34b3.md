+++
title = "Live-verify the Gemini surface (key was unavailable in the bl-8f6a pass)"
created = 1783833462
updated = 1784784882
claimant = "Moats"
priority = 12
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["tests", "live"]
+++
The bl-8f6a live pass (1f81665) verified Anthropic + codex/Responses + Ollama but recorded every Google surface as key-unavailable. Once a Gemini API key exists (prerequisite: user provisions one; wire it like the other live rows — config override, never committed), run the deferred passes:

- thoughtSignature echo (bl-61a9 / CR-G2): a live gemini-2.5 function-calling turn with thinking — decode thoughtSignature -> ToolUse.signature, fold, re-encode, REPLAY with the tool result, assert acceptance (mirrors the anthropic replay proof; offline fixtures cannot prove it).
- :countTokens (bl-24e5): live POST, assert N sane vs the same request's live usage input tokens (anthropic precedent: exact match).
- Structured output on gemini (bl-0333 wire spellings were written from docs — confirm live).
- Image{Url} total-reject (bl-97a7 / CR-G3): local encode behavior, but confirm the mimeType-gap reasoning still matches the current live API docs/behavior before the 0.1.0 freeze.
- Replace any synthetic gemini goldens with verbatim recorded captures (scrub per fixture discipline), same as the anthropic thinking golden replacement.

Record per-surface status in the close message (verified / still-unavailable / synthetic-remains). Precedents: tests/live_* harnesses, live_support/, the bl-8f6a close. Sibling gap NOT in this ball: openai-chat response_format is also key-unavailable (no plain OpenAI key; the codex row is Responses-dialect) — file separately if a key ever exists.