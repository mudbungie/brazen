+++
title = "Auto-install bz locally when main advances"
created = 1784954647
updated = 1784954647
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
The reference-transaction hook already pushes main on delivery. Extend it to also install the bz command locally from the new main tip: a 'make install' target (Makefile = single source of truth for WHAT install is), invoked by the hook (which decides WHEN). Must build from the ref, not the stale root checkout, and must not block the ref transaction.