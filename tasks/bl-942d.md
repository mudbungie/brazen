+++
title = "Routing precedence design question: should an exact model_aliases spelling outrank a model_prefixes family claim?"
created = 1783746505
updated = 1784337541
claimant = "Breviaries"
priority = 8
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
RULED (user sign-off 2026-07-17): adopt GREEDY FIRST-MATCH over a priority list. NOT the tiered alias>prefix rule this ball proposed, and not a specificity comparator — the config IS a priority list, routing takes the first owning row, and AmbiguousModel is RETIRED (a total order always has a first element; no tie to surface).

Priority = declaration order, higher layer's rows first (the general form of the rule default_provider already used for its head). Carried as PartialConfig.provider_order: Vec<String>, merged by concat-dedup (merge_order). default_provider DISSOLVES into provider_order.first(). Dump emits rows in provider_order — this replaces the sort_by_key hack at dump.rs:128 that floated the default row first, which round-tripped a head, not a list.

Design = the spec amendments in this worktree (the specs ARE the living document): architecture.md 4.3 + new 4.3.1, config.md 2/2.2/3.2/7/8, ingress.md 6 (recipe back to ONE line), README, plus corpus sweep (auth.md 5.2, model-discovery.md 5, canonical-protocol.md exit table).

Verified pre-change against the real binary: alias+prefix collision on gpt-4o => 'matches multiple providers (anthropic, openai)', exit 78; claude-… => anthropic; gpt-5.4 => openai.

Spun out: bl-a367 (specs advertise a nonexistent -m short flag; --model is long-form only).