+++
title = "Routing precedence design question: should an exact model_aliases spelling outrank a model_prefixes family claim?"
created = 1783746505
updated = 1783746505
priority = 8
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Raised by bl-6cb4: ingress.md par.6's one-line masquerade recipe (alias gpt-4o on the anthropic row) 78s as ambiguous because the shipped openai default row owns gpt-* via model_prefixes and an alias does NOT outrank a prefix — the recipe now requires additionally emptying openai's model_prefixes (ingress.md + README amended to the working recipe by bl-6cb4). Candidate fix: specific-beats-family precedence — an exact model_aliases spelling outranks any model_prefixes claim; the two-matches=78 ambiguity rule (architecture.md par.4.3, a deliberate never-silent-pick safety decision) stays for alias-vs-alias and prefix-vs-prefix ties. Recommendation: adopt it — an exact spelling is strictly more specific than a family claim, it is not a SILENT pick when specified in architecture.md, and it restores the masquerade recipe to one line with no second routing surface. DESIGN task: rule on it, amend architecture.md par.4.3 + config.md par.7 + ingress.md par.6 + revert the recipe, then implement in the same ball (small: one resolution-order change + ambiguity tests). USER SIGN-OFF WANTED before claiming: this relaxes an explicit safety rule.