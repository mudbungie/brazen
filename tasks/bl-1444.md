+++
title = "Update auth spec §10.7: record the VALIDATED OpenAI ChatGPT-SSO live-flow findings (was a risk checklist)"
created = 1781674385
updated = 1781674385
tags = ["docs"]
+++
Update `specs/auth.md` §10.7 to record the VALIDATED outcomes of the OpenAI ChatGPT-SSO live flow (validated 2026-06-16). §10.7 currently frames these as "risks the live flow MUST confirm" / "the validation checklist, not open design" — they are now confirmed, so the living doc is stale (AGENTS.md: amend and correct previous conclusions).

## Findings to record
- **Login handshake validated end-to-end:** `account_id` derived from the id_token claim `https://api.openai.com/auth.chatgpt_account_id`; `expires_at` parsed from the access-token JWT `exp` (OpenAI returns NO `expires_in`, so the §10.3 fallback is load-bearing, not theoretical). Full scope granted; cred stored 0600.
- **Data plane validated:** auth accepted (no 401/403) with `Authorization: Bearer` + `ChatGPT-Account-ID` + `originator`; brazen `openai_responses` body shape ACCEPTED by `chatgpt.com/backend-api/codex/responses`.
- **Codex backend mandatory fields** (each omission → 400 with descriptive `detail`): non-empty `instructions` (from system); `stream: true`; explicit `store: false`. Working model `gpt-5.4`; the `-codex` variants are GATED for a ChatGPT account ("… model is not supported when using Codex with a ChatGPT account").
- **One risk still OPEN:** the form-encoded vs JSON refresh question (§10.7 risk #1) is NOT yet confirmed — the token is ~10 days out so no refresh has fired. Keep it flagged as the lone open item.
- **New gap discovered:** brazen swallows the non-2xx response body (empty message/null provider_detail) — link the error-body fix ball.

## Deliverable
Rewrite §10.7 from "checklist" to "results + the one open item", cross-linking commits 7beffef (design) / bccc73b (impl) and the error-body ball. Docs-only; no code.