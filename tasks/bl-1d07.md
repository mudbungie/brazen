+++
title = "smoke: stdin canonical-request input channel (not just the positional prompt)"
created = 1781678204
updated = 1781678204
parent = "bl-8cae"
priority = 3
tags = ["smoke"]
+++
Every smoke row uses the positional-prompt constructor. The other request channel — parse(stdin) of a canonical JSON request — is never smoke-exercised live. Add a probe per provider that pipes a minimal canonical request on stdin and asserts a clean stream, covering the read_request/parse path end to end.