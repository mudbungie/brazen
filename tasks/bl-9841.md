+++
title = "release-plz.yml: explicit fork-guard if + 'why this is safe' comment"
created = 1782204028
updated = 1782204028
parent = "bl-3d74"
tags = ["ci", "lane1"]
+++
release-plz.yml grants contents+pull-requests:write workflow-wide and triggers publish on workflow_run; safety vs fork-PR abuse is implicit (workflow_run is branches:[main]-gated). FIX: add an owner guard (e.g. if github.repository_owner == mudbungie) to the jobs plus a one-line comment explaining why fork PRs cannot abuse the write token. Shares release-plz.yml with T2 — same lane, sequential.