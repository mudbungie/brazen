+++
title = "README: repoint the dangling 'Ambient credential discovery' cross-reference"
created = 1782204023
updated = 1782204023
parent = "bl-3d74"
tags = ["docs", "lane1"]
+++
README.md:170-171 says '(see Ambient credential discovery earlier in this README)' but no such section exists (grep -i ambient README.md shows only these two lines). Feature is real (lib.rs:74) but its prose home specs/auth.md §5.5 is excluded from the tarball, so a crates.io reader hits a dead pointer. FIX: repoint to specs/auth.md §5.5 (matching the link convention at README:173) or add the short section.