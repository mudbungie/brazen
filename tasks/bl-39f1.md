+++
title = "Adopt the leak-scan stack from rust-bootstrap"
created = 1786683227
updated = 1786683227
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Port scripts/leak-rules.sh + leak-scan.sh + leak-fixtures/ (rust-bootstrap bl-2c4e template, from yog bl-1043/bl-167d), wire make leak-scan into lint and ci.yml, add .github/workflows/store-scan.yml. The machine-global bl-leak-gate then gates every ball write here automatically (scanner presence is the opt-in). Tune EXCEPTs to this repo's fixtures before landing: the scan must pass main clean first (see the vendor-id scrub ball).