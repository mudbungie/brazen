+++
title = "Responses: project provider-typed tools verbatim (image_generation unreachable); provider tool name optional"
created = 1790393353
updated = 1790393354
claimant = "Incomes"
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Found 2026-09-25 while live-testing image output on openai-chatgpt: (1) openai_responses encode tools_value REJECTS every Tool::Provider with ParseInput/64 ('provider-typed tools are not projected for this dialect'), so the documented request path for OpenAI image output (providers §9 CR-Img: '{type:image_generation} … Tool::Provider, passed verbatim') was false — the spec claimed a capability the encoder refused. (2) the canonical Provider tool REQUIRES name, but Responses native tools (image_generation, web_search, code_interpreter…) carry none. Fix: name optional (absent ≡ empty string, one 'no name' fact like model; serialized only when non-empty); Responses projects Provider as {type: kind, name?: name, ...config}; Anthropic emits name only when non-empty (unchanged for every existing input). Chat/Google/Ollama keep rejecting.