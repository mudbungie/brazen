+++
title = "oauth: client_secret row field, sent on every Grant (auth.md §7.1/§7.5/§8/§11)"
created = 1790392643
updated = 1790392643
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Lane A of bl-cbd4. Add #[serde(default)] pub client_secret: Option<String> to OAuthConfig (src/auth/oauth_row.rs; deny_unknown_fields so the TOML key is accepted); build_token_exchange_request (src/auth/wire.rs) appends ("client_secret", v) to the pairs of EVERY Grant arm when Some — one builder, one extra pair; None ⇒ byte-identical bytes (existing golden tests untouched). Tests: per-arm assertion with the secret present; the absent case pins today's bytes; a config parse test that the TOML key round-trips (dump-config too if OAuthConfig is dumped). Files: src/auth/oauth_row.rs, src/auth/wire.rs, their tests under src/tests/. Do NOT touch src/protocol/.