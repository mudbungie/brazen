+++
title = "image output knob: --image (Google responseModalities, Responses image_generation tool, loud reject elsewhere)"
created = 1790395737
updated = 1790395744
claimant = "Incomes-842b"
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Operator ruling 2026-09-25: hand-built JSON for image output is a gap; image output IS one intent with two dialect spellings, so it is a lifted knob (reverses architecture.md §3.2 image bullet point 3 and providers §9 CR-Img 'No lifted knob'). Design: canonical CanonicalRequest.image: Option<bool> (the SEVENTH lifted knob); --image flag (bool, no value), BRAZEN_IMAGE env, config-file image = true, JSON request key image; Some(true) projects, None/Some(false) omit. Google (and google_cloudcode via body_map): generationConfig.responseModalities = [TEXT, IMAGE], typed wins over extra. OpenAI Responses: append {type: image_generation} unless a Tool::Provider of kind image_generation is already present. Anthropic / OpenAI chat / Ollama / claude_code: Some(true) → ParseInput/64 (no image output on those wires). Tuning gains image: bool (list-providers column, protocol_tuning proves it against encode). unsupported_body_keys 'image' strips it. Model choice stays the caller's (-m); no model-name sniffing.