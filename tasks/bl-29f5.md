+++
title = "protocol: google_cloudcode — google_genai body/chunk in the {model,request}/{response} envelope (providers.md §4.10)"
created = 1790392644
updated = 1790392654
claimant = "Incomes-29f5"
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Lane B of bl-cbd4. New ProtocolId::GoogleCloudCode (serde rename google_cloudcode) in src/config/provider.rs; registry arm in src/registry.rs; new module src/protocol/google_cloudcode/{mod.rs, encode.rs, decode.rs} (<300 lines each, ~200 thickness): encode = WireRequest POST {base_url}/v1internal:streamGenerateContent?alt=sse (stream) | /v1internal:generateContent, body {"model": ctx.model, "request": google_genai::encode::body_map(req)?} (make body_map pub(crate)); path() = the streaming path; decode(frame) = parse the frame JSON, take v["response"] if it is an object else v, then google_genai::decode's chunk fold (make chunk + the non-2xx error path pub(crate) — reuse decode::decode's error handling for non-2xx frames; do not duplicate); decode_full = same unwrap over the single JSON; framing Sse; tuning/shapes = google's; models_shape None; count_tokens default None. Fixture-driven tests from the live capture shapes in specs/auth.md §11.4 / providers §4.10: text stream of 4 chunks with thought part + thoughtSignature part + finishReason; image chunk with inlineData image/jpeg beside a thoughtSignature on the same part → ContentKind::Image; non-stream body; a 403 {"error":…} body maps like google; envelope has exactly two keys; extra folds INSIDE request. Also protocol_tuning / list-providers tests that enumerate ProtocolId must include the new id. Files: src/config/provider.rs, src/registry.rs, src/protocol/mod.rs (mod line), src/protocol/google_cloudcode/*, src/protocol/google_genai/{encode/mod.rs,decode/mod.rs} visibility only, tests. Do NOT touch src/auth/.