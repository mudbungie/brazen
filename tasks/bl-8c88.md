+++
title = "Reconcile specs + README with landed implementation"
created = 1781648927
updated = 1781648927
priority = 55
tags = ["doc"]
+++
Doc-only alignment pass (code is correct; the docs drifted):

1. anthropic-messages.md §4.2 — extend the mid-stream error.type -> ErrorKind table to match the impl (src/protocol/anthropic/decode.rs:198-212): authentication_error/permission_error -> Auth/77; invalid_request_error -> Provider{400}; billing_error -> 402; not_found_error -> 404; request_too_large -> 413; rate_limit_error -> 429; api_error -> 500; timeout_error -> 504; overloaded_error -> 529; otherwise -> Transport/69. This changes the retryable set vs the old 3-bucket text — state it. (Mapping lives in the adapter, not core — correct.)

2. architecture.md §4.4 — registry sketch still lists three structs ApiKeyAuth/BearerAuth/OAuth2Auth; code merged api_key+bearer into one StaticSecretAuth behind two ids (commit 8816ddb). Update §4.4 to match auth.md §3.

3. auth.md — references an AuthError enum (§3.1/§6/§9) that was never built; code uses free-form CanonicalError{kind:Auth}. Remove/repair the AuthError references.

4. config.md §7 — loosen the UnknownProtocol/UnknownAuth promise: resolution does NOT pre-check against the registry; an unknown protocol/auth id fails closed at DISPATCH (registry.rs:45 returns None), not at config time. Drop the config-time-validation claim and the two ConfigError variants from the spec; document dispatch-time fail-closed.

5. config.md — add the `system` field to the PartialConfig schema so a config-/flag-sourced leading system prompt is specced (arch §4.4/§5.5/Decision 10 already reference req.system being config-sourced). NOTE: code still lacks the field in PartialConfig/ResolvedConfig/fill_absent — flag a small follow-up impl ball if we want the convenience; the body-supplied req.system path already works, so not a harness blocker.

6. README.md — Status section is stale: HttpTransport (rustls/ureq), the run() spine, SIGPIPE, and XdgCredStore (0600) all landed (commits 4c4b5f4, 514c01a). Update to reflect reality; what remains spec-only is OAuth2 (bl-3c36) and the extra providers/protocols (bl-2f13).