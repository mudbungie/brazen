+++
title = "Spec drift residue from the bl-0f77 sweep: provider_detail phantom, CR id namespacing, providers.md par.9 buckets"
created = 1783833464
updated = 1783833464
priority = 14
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["docs", "spec-drift"]
+++
Docs-only. The bl-0f77 coherence sweep (677a0cc) flagged three items it deliberately did not change to avoid over-editing; resolve each with a real decision, not a bigger hedge:

1. providers.md par.3.5/par.5.7 say dropped token/duration detail 'rides provider_detail', but Usage has NO provider_detail field — the sentence describes an unbuilt mechanism as if decided. Either write it as an honest open CR (deferred: no field until a consumer exists) or delete the claim; do not add the field (don't store what you can compute has a cousin: don't promise what you haven't built).

2. CR ids are numbered per-file, so openai-chat-mapping's CR-2/CR-5/CR-6 collide with anthropic-messages' CR-2/CR-5 as bare citations. Pick one: namespace the ids (the newer CR-O3/CR-G2/CR-R3 style is already the emergent convention — migrate the bare ones) or state the per-file scoping rule once in each file's CR section header AND fix any cross-file citation that uses a bare id. Sweep all cross-references after.

3. providers.md par.9 keeps several resolved CRs under a 'Deferred / watch items' heading (each labelled resolved inline) — rebucket into a Resolved subsection or drop them; a resolved item under a watch heading misleads a skimmer.

One docs-only commit; close message lists fixes by file+section. Verify every current citation against the tree at claim time — the corpus moves.