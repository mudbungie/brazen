+++
title = "src/testing/mod.rs: replace the stale 'publish = false' rationale"
created = 1782204040
updated = 1782204394
claimant = "Lane3"
parent = "bl-3d74"
tags = ["docs", "lane3"]
+++
testing/mod.rs:4-6 says 'Always compiled (the crate is internal, publish = false)'. There is no separate crate (pub mod testing in lib.rs:40) and Cargo.toml has no publish key (it IS published). Pre-collapse residue. The 'always compiled' conclusion is right; the reason is wrong. FIX: e.g. 'Always compiled (a plain pub mod, not cfg(test), so integration tests and the bin share these doubles).'