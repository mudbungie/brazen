+++
title = "HttpTransport (rustls/ureq) over the Transport seam"
created = 1781559071
updated = 1781559089
priority = 61
tags = ["impl"]

[[blockers]]
id = "bl-ea70"
on = "claim"
+++
Implement the real blocking HTTP client in the bin: ureq-class + rustls + webpki-roots, status peek into TransportResponse, into_reader() chunk iterator mapped to Iterator<io::Result<Bytes>>, connect/DNS/TLS/timeout -> Transport error. Keep it confined to bin (or a feature-gated lib module excluded from the lib coverage gate). Smoke-tested manually against a live provider; the lib stays network-free.