+++
title = "Spec drift: architecture.md §4.3 and config.md §7 advertise a nonexistent `-m` short flag for --model"
created = 1784337076
updated = 1784337076
priority = 4
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
architecture.md par.4.3 and config.md par.7 both cite the routing example `bz -m claude-haiku-4-5-20251001 "q"`, but bz has NO `-m` short flag — `--model` is long-form only. Verified: `bz -m claude-… "hi"` → `unknown flag \`-m\` (try bz --help)`, exit 64. Fix: either add the `-m` alias to the flag parser (cheap, matches muscle memory) OR correct every spec occurrence to `--model`. Grep `-m ` across specs/ before deciding — pick one and make specs match the binary. Found while working bl-942d (the routing examples live in the same paragraphs).