+++
title = "bz login/list-models --help (+ --version) must show help; document --browser/device-flow"
created = 1782204020
updated = 1782204020
parent = "bl-3d74"
tags = ["bug", "lane1"]
+++
bz list-models --help yields 'no provider resolved' exit 78 (run/models.rs:55 never checks flags.help); bz login --help yields 'unknown bz login flag --help' exit 64 (auth/login.rs:71-74). main.rs:39 dispatches verbs before the data-plane help short-circuit. Both verbs are advertised in top-level --help, so this is the likely discovery action. FIX: honor flags.help in run_list (emit help, exit 0) and special-case --help/--version in parse_login_args to print 'usage: bz login <provider> [--browser]' to stdout exit 0. ALSO: the --browser flag + the default headless device flow are absent from --help and README:45 — document both. Add test coverage to keep 100% line cov.