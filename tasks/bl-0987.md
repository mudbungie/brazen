+++
title = "Design: image OUTPUT (model-returned images) — canonical kind, decoders, text-mode sink"
created = 1790058460
updated = 1790125231
claimant = "Incomes"
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["design"]
+++
DESIGN — delivered 2026-09-22 as spec text (this ball is docs-only; implementation is 4 lanes: bl-669b canonical kind+delta [gates the rest], bl-c851 Google decoder, bl-490e Responses decoder, bl-d48a text/pretty sink file write).

FINDING: image/PDF INPUT already shipped (-f by extension; every dialect maps or rejects loudly). OUTPUT was silently dropped by both producing decoders (Google: no inlineData arm; Responses: unknown item type → []).

DECISIONS (where each is written):
1. ContentKind::Image{media_type} + Delta::ImageDelta(base64 text fragments, concatenate-then-decode-once) + ContentStop — architecture.md §3.2 bullet 'A model-RETURNED image…'; canonical-protocol.md §3.2 kind table, delta list, §3.3 fold row. v=1 additive.
2. Responses partial_image events are no-ops (progressive renders, not fragments); only the final result decodes — providers.md §3.4.
3. No lifted request knob: Google modality (extra/body_defaults) and Responses image_generation tool (Tool::Provider) are different mechanisms — providers.md §9 CR-Img.
4. Replay = user-turn Image{Base64} (-f bz-<hash>.png); no assistant slot changes; no id on the variant — canonical-protocol.md §3.3, §2.5 unchanged.
5. Text mode: file <dir>/bz-<sha256 hex[..12]>.<ext> in cwd, path on stderr, stdout untouched; content-addressed name (pure, idempotent, no clock/counter/flag); sink takes dir (run passes '.', tests a tempdir); truncated block never written — architecture.md §5.3, interactive-output.md §4/§5, canonical-protocol.md §3.4.
6. Ingress codecs skip the kind (existing Skip arm) — ingress.md §12.
7. Empty set stated for Anthropic / OpenAI chat / Ollama / claude_code.

NO LIVE CAPTURE (recorded in CR-Img): free-tier Google key → 429 limit 0 on gemini-2.5-flash-image; only OpenAI row is ChatGPT-SSO, probe exited 77. Wire shapes from published references; fixture-driven decoder tests.