+++
title = "Reconcile architecture.md §4.1 Protocol trait to shipped models_shape(): bl-eaf9 replaced fn models_path()+fn decode_models() with one fn models_shape()->ModelsShape, but §4.1 still lists the two old methods and says 'Eight methods'"
created = 1782714847
updated = 1782714888
claimant = "Chisel"
priority = 4
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["doc"]
+++
Single-source-of-truth drift introduced by bl-eaf9 (commit 30c0894). The shipped `Protocol` trait (src/protocol/mod.rs:194) now exposes ONE data method `fn models_shape(&self) -> ModelsShape` (struct: path/array_key/id_key/strip, all &'static str) in place of the removed `fn models_path(&self) -> &str` and `fn decode_models(&self, body) -> Result<Vec<Model>>`; decode is now the ONE generic json::decode_models the verb feeds with models_shape() defaults overridden per row by [provider.models]. 

specs/architecture.md §4.1 is the ONLY stale spec (config.md + model-discovery.md were updated by bl-eaf9; verified no other refs). Fix exactly two things in specs/architecture.md §4.1 (the Protocol trait block, ~lines 320-347):
- line 321: 'Eight methods.' -> 'Seven methods.' (models_path+decode_models collapsed to one).
- lines 341-346: replace the two method stubs (models_path + decode_models) with the single `fn models_shape(&self) -> ModelsShape;` carrying a doc comment that mirrors the shipped one: models-discovery DEFAULTS as DATA (GET path, array_key, id_key, Google's strip), overridden per row by [provider.models] (§3.2), fed to the one generic decode that projects an order-preserving Vec<Model>. Match the doc's voice; reference model-discovery §3.1/§3.2.

Scope: specs/architecture.md ONLY. No code. Doc-only close (coverage unaffected).