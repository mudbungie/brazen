+++
title = "Release prep: bump version 0.0.1 -> 0.0.2 + CHANGELOG [0.0.2] (--reasoning), to cut the next crates.io release"
created = 1782721948
updated = 1782721948
priority = 1
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["release"]
+++
0.0.1 is published on crates.io (sparse index: 0.0.0, 0.0.1; tag v0.0.1 @2eaa5c4). The only user-facing change since v0.0.1 is --reasoning (bl-b8df, 540b7f8). Bump Cargo.toml version 0.0.1->0.0.2, sync Cargo.lock (cargo update -p brazen --precise 0.0.2 or cargo build), and roll CHANGELOG [Unreleased]->[0.0.2]-2026-06-29 (keep the existing learn entry, add a --reasoning entry), add a fresh empty [Unreleased], fix the compare link refs. Doc/metadata only; make check must stay green. NOTE: the published 0.0.1 CHANGELOG section is stale vs what 0.0.1 actually shipped (-f/--file, model-discovery override) — that is the release owner's (Dialectic/bl-1d1b) reconciliation, NOT this task; do not touch the [0.0.1] section.