+++
title = "Model discovery impl 4/4: serve probe orchestration + bz list-models verb"
created = 1781802894
updated = 1781802894
priority = 43

[[blockers]]
id = "bl-76e3"
on = "claim"

[[blockers]]
id = "bl-00e7"
on = "claim"

[[blockers]]
id = "bl-67e2"
on = "claim"
+++
Per specs/model-discovery.md §2,§5.2. Integration; needs 1+2+3.

- serve: when cfg.probe, before encode: build WireRequest::get({base_url}{models_path}), stamp timeouts, auth.apply, transport.send, read whole 2xx body (non-2xx -> from_http_status), decode_models, cfg.model = select_model(&models, &cfg.model); then unchanged path. probe==false sends exactly once.
- bz list-models verb: dispatch in bz/src/main.rs like login (argv[0]=="list-models") -> brazen::list_models. Reuse flag parse + into_resolved(None) for provider; GET models_path; auth; decode_models; print. --json -> {"models":[...]}; default text -> ids one-per-line, default suffixed ' (default)'. Exit 0/78/77/69-70 to stderr.
Tests: MockTransport two-send (GET models then chat, assert expanded model in 2nd); list-models run-level (json + text + NoProvider/auth/non-2xx). 100% cov; <=300 lines/file.