+++
title = "CI red: purity native_host_exposure test fails on Windows (CRLF)"
created = 1784954391
updated = 1784954406
claimant = "LanePurity"
priority = 3
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
tests/purity.rs:122 native_host_exposure_is_feature_gated fails on both windows-msvc CI targets: it matches src.contains("#[cfg(feature = \"native-host\")]\npub mod native;") with a hard \n, but Windows runners check out with CRLF so ']\r\npub' never matches. Fix: normalize \r out of the read source before matching in the test (prefer that over .gitattributes so the invariant is robust regardless of checkout config). Introduced by bl-547d (1ca5db8). Blocks release-plz publish (green-CI gate).