+++
title = "openai_responses: audit recognized-but-dropped reasoning events (reasoning_text.done, reasoning_summary_part.added/done, reasoning_summary_text.done)"
created = 1781670884
updated = 1781670884
priority = 30
tags = ["test"]
+++
Follow-up to bl-7e50. The §3.4 dispatch in src/protocol/openai_responses/decode/mod.rs handles SOME of the reasoning event family and silently drops the rest via the _ => [] arm — this split is NOT deliberate, it just grew that way.

Currently DROPPED (recognized wire events, yield nothing):
  - response.reasoning_text.done
  - response.reasoning_summary_text.done
  - response.reasoning_summary_part.added
  - response.reasoning_summary_part.done

For each, decide handle-vs-explicit-no-op and make the decision EXPLICIT (a test asserting the no-op, or a handling arm + test) so it is a deliberate contract, not an accidental fall-through:
  - The .done events are PROBABLY correct no-ops: the Thinking block is closed by the item-level response.output_item.done (same discipline as content_part.done / output_text.done being no-ops, §3.4). Confirm + pin with a test.
  - response.reasoning_summary_part.added is the PER-PART OPEN — it is the exact seam CR-R4 names for a future per-part Thinking representation. If/when per-part lands (see the coexistence ball), this is where the block opens. For now: explicit no-op, documented.

DELIVERABLE: a test (or comment + test) per event making the drop/handle deliberate; a one-line note in §3.4 that the inner reasoning .done/.part events are intentional no-ops, mirroring the existing 'inner .done are no-ops' note for text/function_call.