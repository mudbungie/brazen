+++
title = "Set RELEASE_PLZ_TOKEN so release-PR CI runs unattended"
created = 1784954391
updated = 1784954391
priority = 2
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
USER ACTION (needs a GitHub PAT only mudbungie can mint). Repo secrets have only CARGO_REGISTRY_TOKEN. release-plz.yml header: RELEASE_PLZ_TOKEN 'RECOMMENDED: a fine-grained PAT (contents+PR write) or GitHub App token, so the release PR's commits re-trigger CI.' Today the release PR (chore: release v0.0.4) CI runs sit in action_required and need a manual Approve-and-run per refresh. Do: mint fine-grained PAT scoped to mudbungie/brazen with Contents+Pull-requests write, then gh secret set RELEASE_PLZ_TOKEN.