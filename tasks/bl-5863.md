+++
title = "Ollama: surface message.thinking as a Thinking block (decode) + stop dropping Content::Thinking (encode); add fixture (providers.md §5.4/§5.5)"
created = 1782681476
updated = 1782688981
claimant = "mark"
priority = 3
+++
Confirmed (protocol-wire lane). Additive/forward-compatible (no v=1 bump — a new Thinking block within v=1 is the additive case). Ollama think-enabled output is silently lost end to end:
- DECODE: src/protocol/ollama_chat/decode/mod.rs:~57-61 never reads message.thinking, so a think-enabled response drops its reasoning.
- ENCODE: Content::Thinking is dropped on the request side (a round-tripped transcript loses thinking).
Fix both to mirror the canonical Thinking representation used by anthropic/openai_responses (ContentKind::Thinking / Delta::ThinkingDelta — confirm names in src/canonical/event.rs). Lower stakes than the Google leak (opt-in, no exit-code/refusal angle), but a live code-vs-spec divergence (providers.md §5.4/§5.5) with no reconciling CR note.

CONSTRAINTS: make check close gate (100% line cov etc). Add an ollama thinking fixture (message.thinking) wired into src/tests/ollama_*; keep files <300 lines. Touch only src/protocol/ollama_chat/**, tests/fixtures/ollama_*, src/tests/ollama_*, providers.md.