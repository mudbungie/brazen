+++
title = "Config resolution fold + embedded defaults + --dump-config"
created = 1781559063
updated = 1781559078
priority = 67
tags = ["impl"]

[[blockers]]
id = "bl-26eb"
on = "claim"
+++
Implement config/resolve.rs: PartialConfig (all-Option, sparse provider table), partial_from_env over an injected EnvSnapshot, the flags.or(env).or(file).or(defaults) fold, include_str! defaults.toml parsed through the same path, into_resolved() with the ambiguous-model (78) and missing-provider errors, config-file location resolution, and --dump-config serialization with secret placeholders. Author data/defaults.toml (anthropic + openai rows). Pure, table-tested with injected env maps.