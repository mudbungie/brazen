+++
title = "Extract the shared encoder tail into protocol/json.rs finish_body() (collapse 5 expect-allows to 1)"
created = 1782204033
updated = 1782204033
parent = "bl-3d74"
tags = ["cleanup", "lane2"]
+++
The to_vec().expect(infallible) + WireRequest::new + beta-headers fold is byte-identical across all 5 encoders (openai/encode/mod.rs:53, anthropic/encode/mod.rs:55, openai_responses/encode.rs:48, google_genai/encode/mod.rs:52, ollama_chat/encode.rs:35) — comments even say 'mirrors the Anthropic encode'. FIX: extract one finish_body(body, url, beta) into protocol/json.rs; call from all 5 encoders; collapses 5 clippy::expect_used allows to 1. Do NOT touch protocol/mod.rs (T12 owns it). Keep cov + clippy green.