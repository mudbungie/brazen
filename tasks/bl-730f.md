+++
title = "Implement CLI verb→flag migration: bz login/list-models become --login/--list-models (architecture.md §5.10.4)"
created = 1782592478
updated = 1782592478
priority = 4
tags = ["interface-review", "implementation"]

[[blockers]]
id = "bl-52dc"
on = "claim"
+++
Execute the migration frozen in architecture.md §5.10 / §13.13 (design: bl-52dc). Control verbs move to control-short-circuit flags; data plane untouched; bare-prompt namespace becomes total+frozen.

SHAPES (provider via existing --provider):
- bz login <provider> [--browser]            -> bz --login --provider <id> [--browser]
- bz list-models [--provider X] [--json]      -> bz --list-models [--provider X] [--json]
- --login requires a resolvable provider (78/64 if none); --browser meaningful only with --login
- probes (--help/--version) win first; --dump-config/--list-models/--login mutually exclusive (64 if combined)

CODE:
- src/cli.rs: add login/list_models bools to Flags (recognize --browser), parsed by parse_args
- src/main.rs: route per-mode seam wiring on the --login/--list-models flag (scan up to first --), else run; not argv[0]
- src/auth/login.rs: drop parse_login_args + per-verb --help/--version short-circuit; provider from flags.config.provider
- src/run/models.rs: drop argv[1..] verb-skip + its own short-circuit
- src/run/mod.rs HELP: fold VERBS: block into the control-flag list
- src/auth/refresh.rs + oauth.rs: NotLoggedIn/RefreshFailed hint strings -> 'bz --login --provider <id>'

SPECS: model-discovery.md §2 (verb framing + 'why a verb not a flag' note) and auth.md §7.2 reconcile to architecture.md §5.10 (they defer to architecture.md by their headers).

USER DOCS/SCRIPTS: README.md examples; scripts/smoke.sh (bz login ... --browser).

TESTS: tests/list_models*.rs, tests/login_*.rs, tests/oauth_smoke.rs — change ['list-models', ...] -> ['--list-models','--provider',...] and ['login',<provider>,...] -> ['--login','--provider',<provider>,...].

GATE: make check (100% cov + 300-line cap). Add tests: bare 'bz login'/'bz "models"' is a PROMPT; bz -- --login is the prompt '--login'; --login without provider -> error; two control ops combined -> 64.