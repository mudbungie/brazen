+++
title = "Spec-corpus coherence sweep after the 0.0.3 wave: end-to-end read, fix drift, write down every remaining unwritten decision"
created = 1783491027
updated = 1783491027
priority = 10
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["docs", "spec-drift"]
+++
Concern #3 (+ residue) from the 2026-07-08 arch-review close-out. ~14 concurrent lanes edited specs/ in one night, resolving merge conflicts pairwise; each merge was locally correct but NOBODY has read the corpus end-to-end since. The CHANGELOG [0.0.2]-header loss (caught at release, bl-c092) is this failure class surfacing once. Docs-only ball; zero behavior change.

SWEEP (read every file in specs/ top to bottom against the actual tree at v0.0.3):
- Cross-reference integrity: section numbers cited across files (architecture ↔ providers ↔ anthropic-messages ↔ openai-chat-mapping ↔ config ↔ auth ↔ sse-decoder ↔ model-discovery ↔ interactive-output ↔ protocol-dedup) after many sections were rewritten/renumbered tonight; CR lists (CR-2/CR-5/CR-R3/CR-G2 resolved by bl-61a9, CR-G3 extended by bl-956c, CR-R1 amended by bl-a9e2, new CR-6/CR-O3/CR-Doc/CR-Audio/CR-G3) — no duplicate ids, resolved CRs marked resolved everywhere they're cited.
- architecture.md §11 module layout vs the real src/ tree (tonight added: run/drive.rs, run/count/, canonical/retry_after.rs, canonical/model.rs changes, openai/decode/errors.rs, many test siblings incl. canonical_request_media.rs, reasoning_roundtrip.rs, run_failures_terminal.rs, model_discovery_metadata.rs, count*.rs, openai_stream_terminator.rs).
- README vs the shipped surface: flag list (--timeout single, --raw[=in|out], --count-tokens, --base-url, --reasoning), the Embedding section, install/native-certs note, Releasing section accuracy post-0.0.3.
- CHANGELOG: [Unreleased] fresh and empty, [0.0.3] coherent, compare refs correct.
- §5.10.3 frozen-surface table is EXACTLY the parse_args reality (the 0.1.0 freeze reads from this table — it must be perfect).

WRITE DOWN the still-unwritten decisions the review surfaced, each as one honest sentence/CR in its natural home (verify current text first — some may already exist): (a) the CanonicalRequest grows-only discipline (the request has no version handshake; growth is additive typed fields — state it beside the §3.2 v=1 contract); (b) the unknown-block silent-drop choice on decode (deliberate forward-compat, no diagnostic channel — confirm it is stated, not just implemented); (c) the per-message name / per-block extra-valve tension (bl-956c noted it in §3.1 — confirm adequate); (d) usage reasoning_tokens deferral (providers.md — confirm still accurate post-bl-61a9); (e) anything else found mid-sweep that is implemented but unstated. Deliverable: one docs-only commit; close message lists every fix by file+section.