+++
title = "Extend provider/protocol coverage: Mistral row, responses, Google, Ollama"
created = 1781559073
updated = 1781559092
priority = 59
tags = ["impl"]

[[blockers]]
id = "bl-3c36"
on = "claim"
+++
Per spec 0007: add the Mistral provider row (zero code, the severability demonstration), then OpenAI responses, Google generative-ai (x-goog-api-key, finishReason->End), and Ollama (NDJSON framing, {done:true}->End) as one Protocol impl + one enum arm + one registry insert + rows each. Add golden fixtures and extend the cross-provider same-Vec<Event> property test where a shared logical response exists. Each lands without touching run/resolve/parse/sink/canonical.