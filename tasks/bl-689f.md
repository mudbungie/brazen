+++
title = "README: --raw is implemented (bl-080b closed) — fix stale 'skipped pending bl-080b' + live skip-print"
created = 1782204022
updated = 1782204356
claimant = "Lane1"
parent = "bl-3d74"
tags = ["docs", "lane1"]
+++
README.md:262-263 says the --raw projection is 'currently skipped pending bl-080b (the data-plane raw path sends an empty URL)'. bl-080b is closed/retired 2026-06-17 and serve.rs:110-111 ships the fix; README:70 already lists --raw under 'What works today' (self-contradiction). FIX: README:263 should say --raw is implemented and OFFLINE-tested (run_cache.rs, seams_protocol.rs, sim suite) — do NOT claim live coverage it lacks. Also tests/live_support/mod.rs:182-185 prints a stale 'known gap bl-080b' skip — enable the now-passing live raw check or drop the bl-080b reason. (raw = lossless passthrough: skips encode/decode/model-cache; still does routing+content-type+timeouts+auth; exit from HTTP status.)