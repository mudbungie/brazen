+++
title = "Ingress wave 1: the [ingress] config table + config.md amendment"
created = 1783745036
updated = 1783745061
claimant = "agent-config"
priority = 12
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Implements specs/ingress.md par.6. Deliverable: [ingress] table in the config schema (top-level sibling of [[provider]], deny_unknown_fields like a row): dialect (required-to-serve), listen (default 127.0.0.1:4891), token (Secret, optional), lossy = adapt|reject (default adapt), lossy_overrides map keyed by adaptation NAME with unknown-name = Config error 78 (a typo'd override must not silently leave the default in force). Folds through the same PartialConfig::or ladder as everything else (config.md par.3); --dump-config round-trips it with token redacted like every Secret. Validation at into_resolved: non-loopback listen without token = Config 78 (the refuse-to-start rule, ingress.md par.7). NO new model-routing surface: inbound model resolves through the existing alias/prefix ladder (ingress.md par.6). Also amend specs/config.md to document the table (derive from ingress.md, cite not restate). Touch only src/config/* + specs/config.md to stay disjoint from the other lanes. 100% coverage, 300-line cap.