+++
title = "Ingress wave 1: openai_chat request decoder (dialect JSON -> CanonicalRequest) + ingress module skeleton"
created = 1783745036
updated = 1783745053
claimant = "agent-decode"
priority = 12
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Implements specs/ingress.md par.2 (decode_request contract) and par.3 rungs 1-2-4 at the input edge. Deliverables: src/ingress/ module skeleton (mod.rs, IngressError type, dialect registry stub mirroring the egress protocol registry pattern) + src/ingress/openai_chat/decode.rs mapping the chat/completions request shape onto CanonicalRequest: messages (system/user/assistant/tool roles), content parts (text, image_url), tool_calls -> ToolUse, role tool -> ToolResult, tools -> Tool::Custom (incl function.strict), tool_choice -> ToolChoice, response_format -> output, reasoning_effort -> reasoning, stream/stream_options kept for the encoder's shape decision, temperature/top_p/max_tokens/stop -> typed fields, unknown top-level keys -> extra valve verbatim (forwarded-not-rejected, architecture.md par.3.1). Structural impossibilities reject with ParseInput named per the adapt-or-reject ladder. Replay-stash re-injection joins are OUT of scope here (bl stash ball owns the stash; the decoder exposes the join points: assistant turns keyed by tool-call id / content hash). Tests: request-fixture goldens dialect->canonical; the round-trip property vs the egress openai_chat encoder (ingress.md par.14). 300-line cap: split decode by seam if needed. 100% coverage.