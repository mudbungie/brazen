+++
title = "Model discovery impl 2/4: Protocol::models_path + decode_models for all 5 protocols"
created = 1781802892
updated = 1781802892
priority = 44

[[blockers]]
id = "bl-76e3"
on = "claim"
+++
Per specs/model-discovery.md §3.1. Trait + all 5 impls land together (exhaustiveness; no dead default).

Add to trait Protocol: `fn models_path(&self) -> &str` and `fn decode_models(&self, body: &[u8]) -> Result<Vec<Model>, CanonicalError>` (pure, order-preserving).
Per §3.1 table:
- OpenAiChat (openai, mistral): /models, data[].id
- OpenAiResponses: /models, data[].id
- AnthropicMessages: /v1/models, data[].id
- GoogleGenAi: /v1beta/models, models[].name (STRIP leading 'models/')
- OllamaChat: /api/tags, models[].name
Per-protocol offline fixture tests (sample body -> ordered Vec<Model>; malformed -> Provider error). 100% cov; <=300 lines/file.