+++
title = "smoke: OAuth2 / SSO data-plane path (anthropic OAuth bearer + openai ChatGPT-SSO/Codex)"
created = 1781678200
updated = 1781678200
parent = "bl-8cae"
priority = 1
tags = ["smoke"]
+++
AuthId::OAuth2 and the `bz login` flow have ZERO live smoke. No oauth2 provider row even ships in defaults.toml — anthropic defaults to api_key (x-api-key), which rejects the Max OAuth token (sk-ant-oat01…) we actually have on this box. Needs: an oauth2 row (or --config override) per SSO provider, a stored cred (or login), and a probe that asserts a clean stream. The bearer+oauth-beta override recipe + Codex specifics are in the agent memory (bz-live-anthropic-testing, bz-live-openai-chatgpt-testing). This is the highest-value gap — the bug class we just fixed lives here.