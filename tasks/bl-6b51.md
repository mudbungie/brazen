+++
title = "CI/CD: replace the hand-rolled release.yml with release-plz (automated version/changelog/publish) + prebuilt cross-target binary GitHub Releases"
created = 1781927064
updated = 1781927064
priority = 2
tags = ["impl"]

[[blockers]]
id = "bl-c1e2"
on = "claim"
+++
Depends on collapse (bl-c1e2) + ideally metadata (gated on bl-c1e2 only; coordinate with the metadata task).

## release-plz
- Add release-plz (release-plz/action) for automated semver bump, CHANGELOG generation, crates.io publish on merge to main, and GitHub Release creation. Replaces the manual 'tag v* → cargo publish' release.yml. Needs CARGO_REGISTRY_TOKEN + a GitHub token; document the one-time repo-settings setup (crates-io environment / secrets) in README.
- Single crate now, so publish is one `cargo publish` (no lib-then-bin ordering).

## Prebuilt binaries (user: 'Yes on prebuilds')
- Add a release job that builds the `bz` binary for the CI target matrix (linux gnu/musl x86_64+aarch64, macos x86_64+aarch64, windows msvc x86_64+aarch64) and uploads them to the GitHub Release (e.g. taiki-e/upload-rust-binary-action or softprops/action-gh-release). README install: cargo install brazen OR download a prebuilt bz.
- Keep ci.yml gate (make check) + matrix build/test. Add llvm-tools-preview to any release toolchain step that runs make check (the cov step needs it; ci gate has it, the old release job omitted it).

## Close gate
Workflows lint/parse; make check green. (Live publish is exercised only on a real tag/merge.)