+++
title = "Model discovery impl 1/4: canonical Model + select_model resolver + WireRequest GET method"
created = 1781802890
updated = 1781802890
priority = 45
+++
Per specs/model-discovery.md §3,§4,§6. Foundation, no trait change yet.

- canonical `Model { id: String, default: bool }` (serde, in canonical/), ordered-list element.
- `WireRequest.method: Method` (enum Method { #[default] Post, Get }) + `WireRequest::get(url)` (empty body, Get). encode keeps Post via new/Default. Real HttpTransport (bz crate, coverage-excluded) + MockTransport (testing) read/record method.
- pure `select_model(models: &[Model], seed: &str) -> Result<String, CanonicalError>`: empty seed -> first default-flagged else models[0]; non-empty -> exact id, else first (list order) containing seed case-insensitively; empty list / no match -> ErrorKind::Config (78). Table-tested from literals.
100% line coverage; <=300 lines/file.