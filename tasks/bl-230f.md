+++
title = "Reconcile 3 specs that still document the never-implemented per-block buffering design"
created = 1782205221
updated = 1782206275
claimant = "Lane4"
parent = "bl-3d74"
tags = ["docs"]
+++
Discovered during T9/bl-f94c (which removed the dead OpenBlock.buffer the code never read). These mapping specs still describe a per-block buffering/accumulate-then-parse design that brazen never implemented (decoders emit fragments directly as ContentDeltas): specs/protocol-dedup.md:59, specs/openai-chat-mapping.md:248, specs/anthropic-messages.md:290-326 (per-kind json/signature/data payload accumulation in OpenBlock.buffer). FIX: reconcile all three to the as-built emit-directly design, same edit already applied to specs/sse-decoder.md in T9. Spec/doc only, no code. These three files were untouched by the cleanup lanes, so this is conflict-free to do anytime.