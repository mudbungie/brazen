+++
title = "Publish metadata go-live: version 0.0.1, license MIT, drop publish=false, add exclude (no tests/specs/.github/.claude/AGENTS.md/Makefile in the tarball), rust-version"
created = 1781927062
updated = 1781927651
claimant = "Dialectic"
priority = 2
tags = ["impl"]

[[blockers]]
id = "bl-c1e2"
on = "claim"
+++
Depends on the crate collapse (bl-c1e2). After collapse there is ONE [package].

## Edits (single crate now, so no bz dep-version lockstep trap — that whole class is dissolved by the collapse)
- version = 0.0.1 (user-chosen first real release; 0.0.0 is the reserved stub, immutable).
- license = "MIT" (user decision; the 0.0.0 stub said 'MIT OR Apache-2.0' but repo+LICENSE are MIT — narrowing is fine, 0.0.0 shipped no code). Confirm LICENSE file is MIT.
- Drop `publish = false`.
- rust-version = "1.xx" in [package] — determine the actual MSRV that compiles (balls pins 1.83; verify ours with a pinned-toolchain build).
- exclude = ["tests", "specs", "scripts", ".github", ".claude", ".githooks", "AGENTS.md", "Makefile", ".gitignore"]. KEEP src/, data/ (data/defaults.toml is include_str!'d at src/config/load.rs:15 — MUST ship), README.md, LICENSE, Cargo.toml. Verify with `cargo package --list` that defaults.toml ships and tests/specs do NOT (this also removes the Anthropic OAuth recipe in tests/oneliner.rs + specs/auth.md from the public tarball — the security-hygiene finding).
- Verify `cargo publish --dry-run` (or `cargo package`) succeeds clean.

## Close gate
make check green; cargo package --list audited.