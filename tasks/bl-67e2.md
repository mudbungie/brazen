+++
title = "Model discovery impl 3/4: ResolvedConfig.probe — owned-vs-probe resolution query"
created = 1781802893
updated = 1781810420
claimant = "Bellwether"
priority = 44
+++
Per specs/config.md §7 + model-discovery.md §5.1. Pure, independent of impl 1/2.

In into_resolved: compute `probe = routing_model.is_none() || (!aliased && !prefix_owned)` for the resolved row (extend to the explicit --provider case, which route() doesn't check today). Store ResolvedConfig.probe: bool. When !probe, model is the final wire id (verbatim/substituted); when probe, model holds the seed (partial verbatim, or "" if absent). Routing errors unchanged (a seed still can't pick a provider -> NoProvider). Table tests: owned full id + exact alias -> false; partial + absent -> true; both explicit-provider and routed cases. 100% cov.