+++
title = "smoke: confirm prefix-routing route-probe green PASS with a standard key (anthropic/openai/mistral/google)"
created = 1781803486
updated = 1781833333
claimant = "Garrick"
priority = 3
tags = ["ergonomics"]
+++
bl-bf1b added the provider-less route probe to scripts/smoke.sh (one `probe route text` per prefix-owning row: anthropic claude-, openai gpt-, mistral mistral-, google gemini-), but its green exit-0 PASS path has never been observed live on this box.

Why bl-bf1b couldn't close that loop: the only live creds present are (1) openai-chatgpt.json — an OAuth2 cred for the CUSTOM openai-chatgpt row (protocol openai_responses), which the probe deliberately EXCLUDES (no model_prefixes; gpt- would collide with the standard openai row -> AmbiguousModel); and (2) local ollama (no prefixes, also excluded — and llama3.2 not even pulled, exit 69). Neither is a standard OPENAI_API_KEY/ANTHROPIC_API_KEY/etc. for one of the four prefix-owning rows, so zero route probes fire.

What WAS verified live (bl-bf1b): the routing mechanism itself, via bad-key runs — `bz --model <m> --api-key dummy` with NO --provider reached the correct backend for all four prefixes (provider-specific auth errors: anthropic 'invalid x-api-key' 77, openai 'platform.openai.com' 77, mistral 'Unauthorized' 77, google 'API key not valid' 69), proving prefix routing resolves end-to-end (not NoProvider). Only the 200/exit-0 tail is unobserved.

Done when: with a standard key for at least one prefix-owning row present, `<KEY>=… bash scripts/smoke.sh` shows a green `PASS  <provider>  route  exit 0, N bytes` line. Cheapest: a real OPENAI_API_KEY (standard, NOT the ChatGPT-SSO OAuth) lights up `openai route`. Pure operator-credential confirmation; no code change expected unless the route probe surfaces a defect.