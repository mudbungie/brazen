+++
title = "integ coverage for -f media attach (sim suite drives the real bz wire body)"
created = 1785642747
updated = 1785642748
claimant = "grouping"
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Follow-up to bl-b8ef (-f media-detection): unit tests cover read_files; nothing drives the whole argv → read_files → encode → HTTP-body chain through the real binary.

Plan:
- sim_support::FakeProvider grows a capture constructor that hands back the drained request body (mpsc), so a test can assert what actually hit the wire; existing serve/serve_status unchanged.
- Move the Sim provider table + fixture/config_for into sim_support (SSOT — two suites, one table).
- New tests/sim_media.rs: for each simulated provider, bz -f red.png -f note.txt 'prompt' → exit 0 + captured body carries the png base64 (+ media type where the dialect keeps it — Ollama drops it); pdf across the four document dialects; Ollama pdf → exit 64 encode reject with NO request reaching the server.