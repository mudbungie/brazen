+++
title = "Canonical protocol as a consumer-facing spec: extract and reframe architecture.md §3 for people building ON bz --json"
created = 1783744935
updated = 1783745061
claimant = "agent-spec"
priority = 14
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
+++
Deliverable: specs/canonical-protocol.md + specs/README.md row. The canonical model is rigorously defined but as INTERNAL architecture for brazen implementers; nobody building against bz --json / canonical stdin has a spec addressed to them. Extract the canonical request shape and the versioned event vocabulary (EVENT_SCHEMA_VERSION) into a standalone, versioned, consumer-facing protocol spec: what a consumer may send, what events they will receive, stability guarantees, additive-change policy, worked examples. architecture.md stays authoritative (single source of truth) — the consumer spec DERIVES and must not contradict; where duplication is unavoidable prefer citation over restatement. Motivation (2026-07-09 discussion): brazen created a de-facto standard; fix is (a) document it for consumers, (b) ingress masquerade (separate ball). Primary consumer: the future harness layer + --json scripters.