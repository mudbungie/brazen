+++
title = "Ship a first-class Anthropic OAuth path (auth/headers as row data, not hand-rolled --config)"
created = 1781679361
updated = 1781682107
claimant = "Towheads"
parent = "bl-ce84"
priority = 1
tags = ["ergonomics"]
user_decision = "RECIPE not built-in (2026-06-17): do NOT add an anthropic-oauth row to defaults.toml; document a recipe + ensure a user-config oauth2 row resolves, like OpenAI SSO. Do not reverse auth.md §7 / architecture.md §13.3. See memory anthropic-oauth-recipe-not-builtin."
+++
No oauth-capable anthropic row ships, so using an OAuth token means hand-writing auth=bearer + Authorization header + the oauth-2025-04-20 beta header in --config. Ship it as row DATA (an anthropic-oauth row, or make `bz login anthropic` provision/select one) so the bearer scheme + version/oauth beta headers come from defaults, not the user. Removes the entire --config TOML from the one-liner. Pairs with the smoke OAuth gap bl-61a6.