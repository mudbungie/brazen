+++
title = "Thickness pass, tests: bring the 36 >200-line src/tests/*.rs files toward sub-200 via real aspect seams (excluded from bl-bce0 for lane-conflict reasons)"
created = 1783145258
updated = 1783145258
priority = 12
root_commit = "5969984c7c332086256b0e88bf4c438431e9946f"
tags = ["refactor", "tests"]

[[blockers]]
id = "bl-42f6"
on = "claim"
+++
Completion of the bl-bce0 mandate (user 2026-07-01: 300 = runaway cap, 200 = thickness indicator). bl-bce0 swept the 12 source files; src/tests/** was excluded because bl-42f6 was rewriting several suites concurrently. 36 test files were >200 at filing (299 run_modes.rs down to 201 google_encode.rs — re-run `wc -l src/tests/*.rs | sort -rn` after bl-42f6 lands; it reshapes anthropic_decode/encode/fixtures, canonical_request/event, google_encode, decode_full_structured).

Method — same discipline as bl-bce0, with one test-specific caveat:
- Split ONLY along real aspect seams (e.g. a config_resolve suite splitting into routing-vs-validation concerns; an encode suite splitting request-shape vs content-block projection). The repo precedent for test siblings exists (native/tests/{mod,ambient}.rs).
- A test suite is naturally ENUMERATIVE: a 280-line file of focused, one-assertion tests is not 'thick' in the design sense. If a file has no aspect seam, LEAVE IT and note it in the close message — do not invent fake splits or scatter a coherent suite. Getting every file under 200 is NOT the goal; removing genuine thickness is.
- Zero behavior change; 100% coverage held; src/tests/mod.rs updated for new modules; no spec edits needed (test layout is not in arch §11's module layout? verify — if §11 or §9.8 notes the test layout, update it).
- Conventions: identical to bl-bce0 (worktree discipline, commit gate, close from repo root, no AI credit, [bl-id] tag).