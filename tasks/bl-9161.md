+++
title = "ci.yml: add --locked to the matrix build/test (parity with the msrv job)"
created = 1782204043
updated = 1782204665
claimant = "Lane3"
parent = "bl-3d74"
tags = ["ci", "lane3"]
+++
ci.yml matrix (~96-97) runs cargo build/test without --locked, while the msrv job (64) has it. FIX: add --locked so the matrix proves the SHIPPED lockfile builds/tests on every target.