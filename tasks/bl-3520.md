+++
title = "architecture.md §11 module tree: add pipeline/pretty.rs, pipeline/style.rs, testing/cache.rs"
created = 1782204038
updated = 1782204284
claimant = "Lane3"
parent = "bl-3d74"
tags = ["docs", "lane3"]
+++
specs/architecture.md §11 (the authoritative src/ map, ~938,974) lists pipeline as only input.rs/parse.rs/sink.rs — omitting pretty.rs + style.rs (the interactive-output pair §5.3 of the same spec describes) — and the testing/ line omits cache.rs (re-exported as MemoryModelCache, testing/mod.rs:14). FIX: add the three files in the existing one-line style.